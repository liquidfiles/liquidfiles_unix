#pragma once

namespace lf {

enum report_level {
    SILENT,
    NORMAL,
    VERBOSE
};

enum validate_cert {
    VALIDATE,
    NOT_VALIDATE
};

enum output_format {
    TABLE_FORMAT,
    CSV_FORMAT
};

}
